# Reusable release workflow for plugins distributed through this marketplace.
#
# Plugin repos expose it behind a manual trigger, pinned to a commit SHA of this
# repo (never @main — bump the pin deliberately, or let Dependabot do it):
#
#   name: Release
#   on:
#     workflow_dispatch:
#       inputs:
#         bump:
#           description: >-
#             Semver component to bump (patch, minor, or major), or auto to derive
#             it from the conventional commits since the last tag
#           type: choice
#           options: [auto, patch, minor, major]
#           default: auto
#   jobs:
#     release:
#       uses: gwenneg/claude-ichiba/.github/workflows/plugin-release.yml@<sha> # <version/date>
#       with:
#         bump: ${{ inputs.bump }}
#       permissions:
#         contents: write
#       secrets:
#         ICHIBA_PAT: ${{ secrets.ICHIBA_PAT }}
#
# What a release does, in order:
#   1. Compute the next version: latest v* tag + bump.
#   2. Commit it to plugin.json — a Contents-API commit on a release branch,
#      squash-merged right away; both commits are GitHub-signed, satisfying
#      default branches that require signed commits and changes through PRs.
#   3. Tag that commit — a release tag always contains its own version.
#   4. Publish the GitHub release.
#   5. Dispatch back here; update-marketplace.yml opens the marketplace PR.

name: Plugin release

on:
  workflow_call:
    inputs:
      bump:
        description: >-
          Semver component to bump (patch, minor, or major), or auto to derive
          it from the conventional commits since the last tag
        type: string
        required: true
    secrets:
      ICHIBA_PAT:
        description: Token allowed to send repository_dispatch to gwenneg/claude-ichiba
        required: true

jobs:
  release:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:

      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          fetch-depth: 0 # full history so all v* tags are visible

      - id: next-version
        name: Compute next version from the previous tag
        env:
          BUMP: ${{ inputs.bump }}
        run: |
          PREVIOUS_TAG=$(git tag --list 'v*' | sort -V | tail -1)
          COMMITS=$(git log --pretty=format:'%s' "${PREVIOUS_TAG:+$PREVIOUS_TAG..}HEAD")
          if [ -z "$COMMITS" ]; then
            echo "Error: no commits since ${PREVIOUS_TAG:-the beginning} — nothing to release"
            exit 1
          fi
          echo "Commits since ${PREVIOUS_TAG:-the beginning}:"
          git log --pretty=format:'- %s' "${PREVIOUS_TAG:+$PREVIOUS_TAG..}HEAD"

          if [ "$BUMP" = "auto" ]; then
            # Conventional commits decide the bump: a '!' breaking marker
            # anywhere before the colon → major, any feat: → minor, else patch.
            BUMP="patch"
            while IFS= read -r msg; do
              case "$msg" in
                *'!'*:*) BUMP="major"; break ;;
                feat:*) BUMP="minor" ;;
              esac
            done <<< "$COMMITS"
            echo "Auto-derived bump: $BUMP"
          fi

          PREVIOUS_VERSION="${PREVIOUS_TAG#v}"
          PREVIOUS_VERSION="${PREVIOUS_VERSION:-0.0.0}"
          IFS=. read -r MAJOR MINOR PATCH <<< "$PREVIOUS_VERSION"
          case "$BUMP" in
            major) NEXT_VERSION="$((MAJOR + 1)).0.0" ;;
            minor) NEXT_VERSION="$MAJOR.$((MINOR + 1)).0" ;;
            patch) NEXT_VERSION="$MAJOR.$MINOR.$((PATCH + 1))" ;;
            *) echo "Error: unknown bump '$BUMP'"; exit 1 ;;
          esac
          echo "version=$NEXT_VERSION" >> "$GITHUB_OUTPUT"

      - id: update-plugin-json
        name: Commit version bump to plugin.json through a release PR
        env:
          GH_TOKEN: ${{ github.token }}
          VERSION: ${{ steps.next-version.outputs.version }}
        run: |
          MANIFEST=.claude-plugin/plugin.json
          BASE="${GITHUB_REF#refs/heads/}"
          BRANCH="release/v$VERSION"
          CONTENT=$(jq --arg v "$VERSION" '.version = $v' "$MANIFEST" | base64 -w 0)

          # The base branch only accepts signed commits arriving through pull
          # requests, so stage the bump on a release branch and squash-merge it
          # immediately — the Contents-API commit and the squash commit are
          # both GitHub-signed. Create the branch, or force-move it if a
          # previous failed run left it behind.
          if gh api "repos/$GITHUB_REPOSITORY/git/refs/heads/$BRANCH" --silent 2>/dev/null; then
            gh api "repos/$GITHUB_REPOSITORY/git/refs/heads/$BRANCH" \
              --method PATCH -f "sha=$(git rev-parse HEAD)" -F "force=true"
          else
            gh api "repos/$GITHUB_REPOSITORY/git/refs" \
              --method POST -f "ref=refs/heads/$BRANCH" -f "sha=$(git rev-parse HEAD)"
          fi

          # The file blob SHA guards against a concurrent edit of the manifest.
          gh api "repos/$GITHUB_REPOSITORY/contents/$MANIFEST" \
            --method PUT \
            -f "message=release: v$VERSION" \
            -f "content=$CONTENT" \
            -f "sha=$(git rev-parse "HEAD:$MANIFEST")" \
            -f "branch=$BRANCH" > /dev/null

          PR=$(gh api "repos/$GITHUB_REPOSITORY/pulls" \
            --method POST \
            -f "title=release: v$VERSION" \
            -f "body=Automated version bump for the v$VERSION release." \
            -f "head=$BRANCH" -f "base=$BASE" \
            --jq '.number')
          COMMIT=$(gh api "repos/$GITHUB_REPOSITORY/pulls/$PR/merge" \
            --method PUT \
            -f "merge_method=squash" \
            -f "commit_title=release: v$VERSION" \
            --jq '.sha')
          gh api "repos/$GITHUB_REPOSITORY/git/refs/heads/$BRANCH" --method DELETE
          echo "commit=$COMMIT" >> "$GITHUB_OUTPUT"

      - name: Create annotated tag on the bump commit
        env:
          GH_TOKEN: ${{ github.token }}
          TAG: v${{ steps.next-version.outputs.version }}
          COMMIT: ${{ steps.update-plugin-json.outputs.commit }}
        run: |
          # Annotated (not lightweight) — update-marketplace.yml dereferences the tag object.
          TAG_OBJ=$(gh api "repos/$GITHUB_REPOSITORY/git/tags" \
            --method POST \
            -f "tag=$TAG" -f "message=$TAG" -f "object=$COMMIT" -f "type=commit" \
            --jq '.sha')
          gh api "repos/$GITHUB_REPOSITORY/git/refs" \
            --method POST -f "ref=refs/tags/$TAG" -f "sha=$TAG_OBJ"

      - id: create-release
        name: Create release
        env:
          GH_TOKEN: ${{ github.token }}
          TAG: v${{ steps.next-version.outputs.version }}
        run: |
          URL=$(gh release create "$TAG" --repo "$GITHUB_REPOSITORY" --title "$TAG" --generate-notes)
          echo "url=$URL" >> "$GITHUB_OUTPUT"

      - name: Dispatch to claude-ichiba
        env:
          ICHIBA_PAT: ${{ secrets.ICHIBA_PAT }}
          RELEASE: ${{ steps.create-release.outputs.url }}
        run: |
          # Dispatch the full release URL — claude-ichiba resolves the plugin name, tag, and SHA from it.
          curl -fsSL -X POST \
            -H "Authorization: token $ICHIBA_PAT" \
            -H "Accept: application/vnd.github+json" \
            "https://api.github.com/repos/gwenneg/claude-ichiba/dispatches" \
            -d "$(jq -n --arg release "$RELEASE" '{event_type: "plugin-release", client_payload: {release: $release}}')"
